const SQUARE_COUNT = 14;
const INTENSITY = 2;
const WEIGHT = 10;
const RATE = 1;

function setup() {
  createCanvas(innerWidth, innerHeight);
  background(255, 255, 255);
}

function draw() {
  
}

function mouseClicked() {

}
